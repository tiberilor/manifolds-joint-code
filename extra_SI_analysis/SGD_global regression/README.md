# SGD global regression

This script supports the SI analysis showing that the qualitative improvement in global regression performance across CNN layers is independent of random-projection size. It optimizes the same ridge-regression objective with minibatch SGD and nested cross-validation, allowing the analysis to extend beyond dimensions practical for the exact solver.

